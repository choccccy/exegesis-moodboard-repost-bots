# Secrets (never written to disk — op run resolves op.env references at runtime)
.env

# Persistent data volume (db, attachments, logs)
/data/

# Python
__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
.pytest_cache/

# OS / editor
.DS_Store
